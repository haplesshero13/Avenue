{
  "license": "MIT",
  "scripts": {
    "test": "standard -v --fix && flow && ava"
  },
  "devDependencies": {
    "ava": "^0.23.0",
    "babel-preset-diff": "^1.0.3",
    "babel-register": "^6.26.0",
    "flow-bin": "^0.59.0",
    "puppeteer": "^0.13.0",
    "standard-esnext": "^4.0.1"
  },
  "babel": {
    "presets": [
      "diff"
    ]
  },
  "ava": {
    "babel": "inherit",
    "require": [
      "babel-register"
    ]
  }
}
